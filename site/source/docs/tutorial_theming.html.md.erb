# 主题

## 主题定制

Kickoff的UI组件样式源代码是使用Sass进行幂等式编写的，
这意味这在覆盖内置样式的时候不需要考虑css选择器在文件中的先后顺序，
只需要关注css选择器的优先级就可以了。通过这种方式，自定义的css会更稳健也更容易维护。

如果在项目中使用Sass版本的样式文件，还可以通过覆盖样式变量的方式来修改内置样式。
请确保在引入Kickoff Sass之前引入自定义的Sass变量。
所有样式变量可以在 <%= link_to '这里', "#{data.kickoff.repo}/blob/master/src/sass/_variables.sass" %>
查看。
